% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/2.mark_new_series.R
\name{get.diff.dt.WHOVR}
\alias{get.diff.dt.WHOVR}
\title{Compare the WHO VR dt_new vs. dt_old, count only the countries with new years
of data because if we calculate the value differences, the differences could
be very small and it will depend on the rounding of the difference}
\usage{
get.diff.dt.WHOVR(count_rounding = NULL)
}
\arguments{
\item{count_rounding}{default to NULL, if supply a value, e.g. 6, it will
count the difference also using the diff round to 1E-6}
}
\value{
list of dt1 (the comparison dataset for debugging) and iso_newVR (the
  vector of country isos with different WHO VR)
}
\description{
dir_new_data_U5MR and dir_old_data_U5MR can be loaded by
\code{\link{find.dir.for.VR.comparison}}
}
\details{
Supply `dir_new_data_U5MR` or/and `dir_old_data_U5MR` in the global
environment to overwrite the default selection
}
